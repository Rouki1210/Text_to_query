# Development dependencies — mirrors [project.optional-dependencies].dev
# in pyproject.toml. Includes everything in requirements.txt.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
ruff>=0.6
faker>=28.0               # mock data generation for scripts/seed_mock_data.py
