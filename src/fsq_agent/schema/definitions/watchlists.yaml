# Data dictionary for the watchlists unit.
# GENERATED by scripts/introspect_schema.py — descriptions are placeholders.
# Replace every TODO with business language; that text is what the model
# reads to map plain-English questions onto these columns.

description: >
  User-curated lists of assets being tracked.

keywords: ["watchlist", "tracking", "danh sách theo dõi"]

tables:
  - name: "Watchlists"
    description: TODO — what one row of Watchlists represents
    columns:
      - {name: "Id", type: "integer", required: true, description: "TODO"}
      - {name: "UserId", type: "integer", required: true, description: "TODO"}
      - {name: "Name", type: "character varying", required: true, description: "TODO"}
      - {name: "IsFavorite", type: "boolean", required: true, description: "TODO"}
      - {name: "UserId1", type: "integer", required: false, description: "TODO"}
  - name: "WatchlistItems"
    description: TODO — what one row of WatchlistItems represents
    columns:
      - {name: "Id", type: "integer", required: true, description: "TODO"}
      - {name: "WatchlistId", type: "integer", required: true, description: "TODO"}
      - {name: "AssetId", type: "integer", required: true, description: "TODO"}
      - {name: "AddedAt", type: "timestamp with time zone", required: true, description: "TODO"}

joins:
  - '"WatchlistItems"."AssetId" = "Assets"."Id"'
  - '"WatchlistItems"."WatchlistId" = "Watchlists"."Id"'
  - '"Watchlists"."UserId" = "Users"."Id"'
  - '"Watchlists"."UserId1" = "Users"."Id"'
